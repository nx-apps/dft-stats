module.exports = function (app) {
    var controller = require('../controllers/excel.controller')
    // app.get('/read', controller.read);
    // app.get('/init',controller.init);
    // app.get('/convert',controller.convert);
    // app.get('/dit',controller.dit);
}